{:paths ["src"]
 :deps {
        ;; kotoba.test (clojure.test-compatible surface), not clojure.test --
        ;; scripts/migrate-clojure-test-to-kotoba-test.cljk. Explicit floor.
        io.github.kotoba-lang/test {:git/url "https://github.com/kotoba-lang/test.git"
                                    :git/sha "2ab4885f99fd25552d380d0f6b0b54c5a72eb7ae"}io.github.kotoba-lang/langgraph
        {:git/sha "db3c0ecb0385fd4371a8e8a8118dc4d33c1bd2df"}
        ;; game-production craft lib (ADR-2607023000) — the governor does not
        ;; own design scoring truth: it calls spec/problems and
        ;; balance/clear-rate-required-milli against the request's actual
        ;; gameSpec, and build commits carry the resulting design report.
        io.github.kotoba-lang/game-production
        {:git/url "https://github.com/kotoba-lang/game-production"
         :git/sha "ae08263df1c044ce030af72ec62544ea2476f4e2"}}
 :aliases
 {:test {:extra-paths ["test"]
         :extra-deps {io.github.cognitect-labs/test-runner
                      {:git/tag "v0.5.1" :git/sha "dfb30dd6605cb6c0efc275e1df1736f6e90d4d73"}}
         :main-opts ["-m" "cognitect.test-runner"]}
  ;; static analysis -- clj-kondo from deps (no binary), errors fail CI.
  :lint {:replace-deps {clj-kondo/clj-kondo {:mvn/version "2024.11.14"}}
         :main-opts    ["-m" "clj-kondo.main"
                        "--lint" "src" "test"
                        "--fail-level" "error"]}}}
